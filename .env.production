# URL du back Spring Boot (Render) utilisée au build de production (Vercel).
# Sans cette variable, api.js retombe sur http://localhost:8080 et le menu ne
# se charge pas chez les visiteurs. C'est une URL publique, pas un secret.
VITE_API_URL=https://burger-house-api-xw1a.onrender.com
